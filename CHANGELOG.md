# 変更履歴

このプロジェクトは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

`Movo`（JavaScript 版）とは**別のバージョン番号**を持ちます。JSON の書き方は
互換なので、対応する JS 版の機能水準は各エントリに書きます。

## [0.1.0] - 2026-08-11

Movo（JS 版 1.6.0）の移植を開始しました。**実行速度の最大化**が目的です。

### 追加 — 移植した層

| 先 | 中身 |
| --- | --- |
| `core/` | 画像・PNG/JPEG/BMP・WAV・色・数学・乱数・キャッシュ・設定・歌詞・SVG |
| `schema/` | 検証と正規化（拍/小節・params・継承・バリアント・相対単位） |
| `expression/` | サンドボックス式エンジン |
| `animation/` | キーフレーム・イージング・モジュレーター |
| `renderer/` | ラスタライザ・文字・図形・エフェクト 69 種・3D・粒子 |
| `deformer/` | メッシュ変形 16 種・マスク 10 種 |
| `physics/` | 剛体・拘束・IK・リグ・柔体 |
| `audio/` | BPM 検出・ミックス・ラウドネス・ダッキング |
| `timeline/` | シーンとレイヤーの時間解決 |
| `exporters/` | mp4 / webm / gif / png 連番 |

### 一致 — JS 版と同じ結果が出ることを数値で固定

JS 版を実際に走らせた出力を `tests/data/parity_*.json` に焼き込み、突き合わせています。

- **ラスタライザ・図形・文字・フォント** — 98 ケース**画素完全一致**
- **エフェクト 69 種** — 90 ケース中 87 が完全一致。残り 3 件は `np.arctan2` /
  `np.sin` が libm と 1 ULP 違い、ちょうど `.5` の丸めの向きが変わるだけで
  **式の間違いではありません**（1,120 画素中 4 画素）
- **schema** — 作例 48 本の検証結果が**メッセージ文字列まで一致**
- **物理** — 粒の乱数列そのもの（位置・寿命・シード）を 240 ステップ照合
- **音** — WAV は**バイト単位**、EBU Tech 3341 の −23dBFS 正弦が −23 LUFS ±0.1、
  BPM 既知の自作音源 10 本すべて JS 版と同値
- **GIF** — パレットも符号語も**バイト単位で一致**

一致させるのに **3 種類の丸めを使い分ける**必要がありました。JS の `Math.round` は
`floor(x+0.5)`、`toFixed` は ROUND_HALF_UP、`Uint8ClampedArray` は偶数丸め。
Python の組み込み `round` は偶数丸めなので、そのまま使うと**ずれます**。
JPEG の逆 DCT は**わざと float32 に落として** JS の `Float32Array` に合わせています。

### 速度 — 「NumPy でベクトル化すれば速い」は当てにならない

3 人の担当が独立に同じ壁に当たりました。**gather が支配的な処理は、
中間配列のメモリ帯域に食われて NumPy のほうが遅くなります。**

| | NumPy | Numba |
| --- | --- | --- |
| 多角形の塗り | 30.4 ms | **0.296 ms** |
| 3D LUT の当てはめ | 411 ms | **32 ms** |
| 双一次補間 | 470 ms | **20 ms** |

全画面を一様に触る処理は逆に NumPy が圧勝です（純 Python 720 ms → 13 ms）。
**触る画素が «全部» なら NumPy、«一部» なら Numba** が目安です。

主な実測（純 Python 比）:

| 場所 | 使ったもの | 前 | 後 |
| --- | --- | --- | --- |
| bloom | NumPy | 416.3 ms | **18.7 ms** |
| noise | NumPy | 134.6 ms | **6.0 ms** |
| マスク場 64x64 | NumPy | 40.6 ms | **0.31 ms** |
| 剛体の速度ソルバ | Numba | 3.97 ms | **0.018 ms** |
| K 特性 IIR（30 秒） | Numba | 1,237 ms | **4.76 ms** |
| GIF 減色＋LZW | Numba | 3,952 ms | **10.8 ms** |

物理は**状態を 1 枚の `float64` 配列に置き、`body.position.x` をその窓にする**ことで、
カーネルへ「詰め直しゼロ」で渡しています。

### 安全 — サンドボックスは JS 版より厳しく

JS の `safeGet` は関数のプロパティを読めましたが、Python で同じことを `getattr` で
やると `f.__globals__["__builtins__"]["__import__"]` まで一直線です。
**`safe_get` は `getattr` を一切使わず**、辞書・配列・文字列と明示的にぶら下げた値だけを
読みます。`eval` / `exec` / `compile` は 1 か所もありません。

### 修正 — 移植の途中で見つけたもの

- **JS 版の #78**（ffmpeg が落ちても終了コード 0）を Python 版では最初から塞ぎました。
  シグナル死・EPIPE の握りつぶし・出来たファイルの尺検査の 3 か所。加えて Python 固有の
  罠として、**標準エラーを別スレッドで読み続けます**（読まないとパイプが詰まって固まります）
- `Math.round` と Python の `round` の差で **GIF のパレットが 1 ずれ**、
  **fps 25 のフレーム番号が 1 ずれて**いた
- `apply_animations` が値ではなく `{"value": 9}` を書き込んでいた
- `blend_over` が `astype`（切り捨て）を使っており、JS の `Uint8ClampedArray`
  （最も近い整数へ丸める）に対して**全面で 1 ずれて**いた

### 依存とライセンス

**単体 EXE への同梱を前提**に、すべて再配布可能な寛容ライセンスであることを
確認しています（NumPy BSD-3 / Numba・llvmlite BSD-2 / PyInstaller は GPLv2+ だが
**成果物を任意のライセンスで配布できる例外条項付き** / Python PSF）。
著作権表示は `THIRD-PARTY-NOTICES.md`。

**PNG / JPEG / WAV のコーデック、TrueType のパーサ、式エンジン、物理演算、
BPM 検出は JS 版と同じく自前実装**で、依存を増やしていません。

### 既知の制限

- 拘束（constraint）は Python のまま。種類ごとに書き方が違い、本数がひと桁なので
  実害は無い（1 ステップ全体で 0.28 ms）
- 粒の消滅は JS 版と同じ前詰めなので、1 ステップで大量に死ぬと最悪 O(n²)。
  順番を保つため意図的
- 音量・パンにキーフレームを書くと JS 版と同じくサンプルごとの解決になり遅い
- `Body.id` の既定は `Math.random()` ではなく連番（決定性のため。JS と文字列は不一致）
